/**
 * A Position is used to determine the place within a document tree
 * It's an array of numbers : the numbers are successive indexes used to descend the tree
 */
type Pos = Array<Number> | null
export default Pos
